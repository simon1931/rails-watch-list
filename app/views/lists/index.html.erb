<div class="container">
  <h1>Les Lists</h1>
  <ul>
    <% @lists.each do |list| %>
      <li>
        <%= link_to list.name, list_path(list) %>
      </li>
    <% end %>
  </ul>
  <%= link_to "Nouvelles List", new_list_path, class: "btn btn-primary" %>
</div>
